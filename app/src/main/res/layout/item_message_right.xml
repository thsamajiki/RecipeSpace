<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="messageRight"
            type="com.hero.recipespace.domain.message.entity.MessageEntity" />
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_chat_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@+id/cv_chat_container"
            android:layout_marginBottom="10dp"
            android:layout_toStartOf="@+id/cv_chat_container"
            android:textColor="@color/normal_gray"
            android:textSize="@dimen/small_text_size"
            tools:text="20.06.24" />

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cv_chat_container"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_marginStart="5dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="10dp"
            app:cardBackgroundColor="@color/colorPrimary"
            app:cardCornerRadius="5dp"
            app:cardElevation="3dp"
            app:cardUseCompatPadding="true"
            app:contentPadding="10dp">

            <TextView
                android:id="@+id/tv_chat_content"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxWidth="200dp"
                android:textColor="@color/white"
                android:textSize="@dimen/body_text_size"
                tools:text="@string/dummy_text" />

        </com.google.android.material.card.MaterialCardView>

    </RelativeLayout>
</layout>